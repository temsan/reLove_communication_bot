отвечай всегда на русском
коммиты писать на русском

# Структура проекта
- Не плодить файлы в корне проекта
- Распределять файлы по соответствующим папкам:
  - scripts/ — утилиты и скрипты
  - scripts/analysis/ — анализ данных
  - scripts/finetuning/ — файнтюн моделей
  - scripts/database/ — работа с БД
  - scripts/profiles/ — работа с профилями
  - scripts/telegram/ — интеграция с Telegram
  - scripts/testing/ — тестирование
  - scripts/utils/ — утилиты
  - docs/ — документация и MD файлы по задачам
  - config/ — конфигурационные файлы (если много)
  - data/ — данные и датасеты
- Удалять частные MD файлы по завершении задачи
- Не хранить кэши в проекте (.mypy_cache, __pycache__)

# Принципы кодирования
- Предпочитай простые решения
- Избегай дублирования кода — всегда проверяй кодовую базу на похожую функциональность
- Учитывай разные окружения: dev, test, prod
- Делай только запрошенные изменения или те, в которых уверен
- При исправлении ошибок не вводи новые паттерны без необходимости — сначала исчерпай все варианты существующей реализации
- Удаляй старую реализацию после замены, чтобы не было дублей
- Держи кодовую базу чистой и организованной
- Избегай скриптов в файлах, особенно если они запускаются один раз
- Не допускай файлы больше 200-300 строк — рефакторь
- Mock данные только в тестах, никогда в dev/prod
- Не добавляй fake data в код, который влияет на dev/prod
- Не перезаписывай .env без подтверждения

не дублируй сущности, всегда сначала анализируй кодовую базу