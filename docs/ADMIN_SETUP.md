# 👨‍💼 Настройка админа

Инструкции по настройке админа для управления промптами.

---

## 🔧 Настройка

### Шаг 1: Получи свой ID в Telegram

1. Напиши боту любое сообщение
2. Посмотри логи бота
3. Найди свой `user_id`

Или используй бота: [@userinfobot](https://t.me/userinfobot)

### Шаг 2: Установи переменную окружения

В файле `.env`:
```
ADMIN_ID=123456789
```

Замени `123456789` на свой ID.

### Шаг 3: Перезагрузи бота

```bash
docker-compose restart relove_bot
```

---

## 👁️ Видимость функций

### Только для админа видны:
- ✅ Команда `/admin_prompts` — открыть меню
- ✅ Команда `/my_topic` — выбрать тему через кнопки
- ✅ Команда `/set_topic_for <user_id> <topic>` — установить тему
- ✅ Команда `/clear_history <user_id>` — очистить историю
- ✅ Команда `/stats` — статистика
- ✅ Команда `/topics` — список тем
- ✅ Кнопки для выбора темы

### Для остальных:
- ❌ Команды не видны (молча игнорируются)
- ❌ Кнопки не видны
- ❌ Никаких сообщений об ошибках

---

## 📋 Админ-команды

### Открыть меню
```
/admin_prompts
```

Показывает:
- Описание всех команд
- Список доступных тем

### Выбрать тему через кнопки
```
/my_topic
```

Показывает:
- Текущую тему (если установлена)
- Кнопки для выбора новой темы
- Кнопку "Авто" для автоматического выбора

### Установить тему для пользователя
```
/set_topic_for <user_id> <topic>
```

Примеры:
```
/set_topic_for 123456789 energy
/set_topic_for 987654321 relationships
/set_topic_for 555555555 past_lives
```

Доступные темы:
- `energy` — Энергетическая работа
- `relationships` — Отношения
- `past_lives` — Прошлые жизни
- `business` — Бизнес
- `general` — Общий диалог
- `diagnostic` — Диагностика

### Очистить историю диалога
```
/clear_history <user_id>
```

Пример:
```
/clear_history 123456789
```

### Показать статистику
```
/stats
```

Показывает:
- Количество пользователей
- Количество сообщений
- Среднее сообщений на пользователя

### Показать доступные темы
```
/topics
```

Показывает список всех доступных тем с их ID.

---

## 🎯 Примеры использования

### Пример 1: Выбрать тему через кнопки
```
1. Напиши /my_topic
2. Нажми кнопку "⚡ Энергия"
3. Готово! Теперь все ответы будут в стиле энергетической работы
```

### Пример 2: Установить тему для пользователя
```
1. Напиши /set_topic_for 123456789 energy
2. Система установит тему "Энергетическая работа" для пользователя 123456789
3. Все его ответы будут в этом стиле
```

### Пример 3: Очистить историю
```
1. Напиши /clear_history 123456789
2. История диалога пользователя будет очищена
3. Следующий диалог начнется с чистого листа
```

### Пример 4: Посмотреть статистику
```
1. Напиши /stats
2. Увидишь количество пользователей и сообщений
3. Сможешь оценить активность
```

---

## 🔐 Безопасность

### Команды видны только админу
- Если не-админ напишет команду, она будет молча проигнорирована
- Никаких сообщений об ошибках
- Никаких подсказок

### Кнопки видны только админу
- Если не-админ попытается нажать кнопку, она будет молча проигнорирована
- Никаких сообщений об ошибках

### Логирование
- Все действия админа логируются
- Можешь проверить логи для аудита

---

## 🚀 Быстрый старт

1. **Установи ADMIN_ID в .env**
   ```
   ADMIN_ID=123456789
   ```

2. **Перезагрузи бота**
   ```bash
   docker-compose restart relove_bot
   ```

3. **Используй команды**
   ```
   /admin_prompts — открыть меню
   /my_topic — выбрать тему
   ```

---

## ✅ Проверка

Если все настроено правильно:
- ✅ Команда `/admin_prompts` работает
- ✅ Видишь меню с командами
- ✅ Кнопки в `/my_topic` работают
- ✅ Команды не видны для других пользователей

---

**Готово!** 🎉

