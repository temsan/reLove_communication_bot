# 📚 Полное руководство: Импорт пользователей из каналов reLove

## 🎯 Что это дает?

✅ **Получение участников** из всех каналов и групп reLove  
✅ **Автоматическое заполнение профилей** через LLM  
✅ **Массовая обработка** тысяч пользователей  
✅ **User-клиент** вместо бота (больше возможностей)

## 🔧 Подготовка

### 1. Проверка зависимостей

Telethon уже установлен в `requirements.txt`. Если нет:

```bash
pip install telethon
```

### 2. Настройки в .env

Убедитесь, что есть:

```env
TG_API_ID=21834116
TG_API_HASH=3139c483fb576f2043610eb2ba7e285e
TG_SESSION=relove_bot
```

## 🚀 Первый запуск (Авторизация)

### Шаг 1: Авторизация в Telegram

```bash
python scripts/test_telethon_connection.py
```

**Что произойдет:**
1. Скрипт попросит номер телефона (формат: +79991234567)
2. Telegram отправит код на ваш телефон
3. Введите код
4. Сессия сохранится в `relove_bot.session`

**Важно:** Это нужно сделать **один раз**. После этого сессия будет использоваться автоматически.

### Шаг 2: Проверка подключения

После авторизации скрипт покажет:

```
✅ Successfully connected!

👤 Your account:
   ID: 123456789
   Name: Ваше Имя
   Username: @your_username
   Phone: +79991234567

💬 Your dialogs (first 10):
   1. reLove Community (@reloveinfo) [Channel]
   2. reLove Chat (@relovechat) [Group]
   ...

🔍 Searching for reLove channels...
   ✓ reLove Community (@reloveinfo) [Channel]
   ✓ reLove Chat (@relovechat) [Group]
   ✓ Путь Героя reLove (no username) [Group]

✅ Found 3 reLove channels/groups
```

## 📋 Использование

### Вариант 1: Просмотр доступных каналов

```bash
python scripts/quick_channel_list.py
```

Покажет все каналы reLove, к которым у вас есть доступ.

### Вариант 2: Полный список с деталями

```bash
python scripts/fill_profiles_from_channels.py --list-channels
```

### Вариант 3: Импорт из всех каналов

#### A. Быстрый импорт (только добавление в БД)

```bash
python scripts/fill_profiles_from_channels.py --all --no-fill
```

**Что делает:**
- Находит все каналы reLove
- Получает участников
- Добавляет/обновляет в БД
- **НЕ** заполняет профили (быстро)

**Когда использовать:** Для первичного импорта большого количества пользователей.

#### B. Полный импорт (с заполнением профилей)

```bash
python scripts/fill_profiles_from_channels.py --all
```

**Что делает:**
- Находит все каналы reLove
- Получает участников
- Добавляет/обновляет в БД
- **Заполняет профили** через LLM (медленно)

**Когда использовать:** Когда нужны готовые профили сразу.

#### C. Импорт с ограничением

```bash
python scripts/fill_profiles_from_channels.py --all --limit 100
```

Обработает только первых 100 участников из каждого канала.

### Вариант 4: Импорт из конкретного канала

```bash
python scripts/fill_profiles_from_channels.py --channel @reloveinfo
```

Или по ID:

```bash
python scripts/fill_profiles_from_channels.py --channel -1001234567890
```

## 📊 Результаты

После выполнения вы увидите:

```
============================================================
Processing: reLove Community
============================================================
Getting participants from @reloveinfo...
Found 1250 participants in @reloveinfo
Processing reLove Community: 100%|████████| 1250/1250 [05:23<00:00, 3.87it/s]

============================================================
STATISTICS
============================================================
Channels processed: 3
Users found: 3500
Users added: 3200
Users updated: 300
Profiles filled: 3500
Errors: 0
============================================================
```

## 🔄 Рекомендуемый workflow

### Для первого импорта:

```bash
# 1. Посмотреть какие каналы доступны
python scripts/quick_channel_list.py

# 2. Быстро импортировать всех пользователей
python scripts/fill_profiles_from_channels.py --all --no-fill

# 3. Заполнить профили пакетами (контролируемо)
python scripts/fill_profiles.py --all --batch-size 20
```

### Для регулярного обновления:

```bash
# Обновить пользователей из каналов
python scripts/fill_profiles_from_channels.py --all --no-fill

# Обновить профили активных пользователей
python scripts/fill_profiles.py --all --batch-size 50
```

## ⚠️ Важные ограничения

### 1. Broadcast каналы

Для **публичных broadcast каналов** (где только админ публикует посты) **НЕЛЬЗЯ** получить список подписчиков через API.

**Что можно получить:**
- ✅ Админов канала
- ✅ Пользователей из комментариев (если включены)
- ❌ Полный список подписчиков

**Решение:** Используйте группы или супергруппы вместо broadcast каналов.

### 2. Приватные каналы

Нужно быть **участником** канала, чтобы получить список участников.

### 3. Rate Limits

Telegram ограничивает количество запросов:
- ~20 запросов в секунду
- ~200 запросов в минуту

**Скрипт автоматически делает паузы**, но при большом количестве пользователей процесс займет время.

### 4. Типы каналов

| Тип | Можно получить участников? |
|-----|---------------------------|
| Публичная группа | ✅ Да |
| Приватная группа | ✅ Да (если вы участник) |
| Супергруппа | ✅ Да (если вы участник) |
| Broadcast канал | ❌ Нет (только админы) |
| Канал с комментариями | ⚠️ Частично (из комментариев) |

## 🐛 Troubleshooting

### Ошибка: "Could not find the input entity"

**Причина:** Канал не найден или нет доступа.

**Решение:**
1. Проверьте username канала
2. Убедитесь, что вы участник
3. Попробуйте использовать ID вместо username

### Ошибка: "A wait of X seconds is required"

**Причина:** Telegram rate limit.

**Решение:** Подождите указанное время. Скрипт автоматически продолжит.

### Ошибка: "The channel specified is private"

**Причина:** Нет доступа к приватному каналу.

**Решение:** Вступите в канал сначала.

### Ошибка: "Not authorized"

**Причина:** Сессия не авторизована.

**Решение:**
```bash
python scripts/test_telethon_connection.py
```

### "No reLove channels found"

**Причина:** Нет каналов с "relove" в названии.

**Решение:**
1. Вступите в каналы reLove
2. Проверьте, что каналы содержат "relove" или "релов" в названии
3. Используйте конкретный username: `--channel @reloveinfo`

## 📝 Логи

Все логи сохраняются в:

```
logs/fill_profiles_from_channels.log
```

Формат:
```
2024-01-15 10:30:45 - INFO - Found 3 reLove channels
2024-01-15 10:30:50 - INFO - Getting participants from @reloveinfo...
2024-01-15 10:31:20 - INFO - Found 1250 participants
2024-01-15 10:31:25 - INFO - Added new user 123456 (@username)
...
```

## 🔐 Безопасность

⚠️ **ВАЖНО:**

1. **Не делитесь файлом `.session`** - это полный доступ к вашему аккаунту
2. **Используйте отдельный аккаунт** для автоматизации (не основной)
3. **Соблюдайте Terms of Service** Telegram
4. **Не спамьте** пользователей
5. **Храните `.session` в `.gitignore`** (уже добавлено)

## 🔄 Автоматизация

### Cron job (Linux/Mac)

```bash
# Каждый день в 3:00 утра
0 3 * * * cd /path/to/project && python scripts/fill_profiles_from_channels.py --all --no-fill
```

### Task Scheduler (Windows)

1. Откройте Task Scheduler
2. Create Basic Task
3. Trigger: Daily, 3:00 AM
4. Action: Start a program
5. Program: `python`
6. Arguments: `scripts/fill_profiles_from_channels.py --all --no-fill`
7. Start in: `C:\path\to\project`

### Фоновые задачи бота

Уже настроены в `relove_bot/tasks/background_tasks.py`:
- `profile_rotation_task()` - обновление профилей
- Запускается автоматически при старте бота

## 🎯 Проверка результатов

### В боте (для админов):

```
/admin_find_users limit=20
/admin_user_info user_id=123456
```

### В базе данных:

```sql
-- Количество активных пользователей
SELECT COUNT(*) FROM users WHERE is_active = true;

-- Пользователи с заполненными профилями
SELECT COUNT(*) FROM users WHERE markers->>'summary' IS NOT NULL;

-- Последние добавленные
SELECT id, username, first_name, registration_date 
FROM users 
ORDER BY registration_date DESC 
LIMIT 10;
```

### В Python:

```python
from relove_bot.db.session import async_session
from relove_bot.db.models import User
from sqlalchemy import select

async with async_session() as session:
    result = await session.execute(select(User).where(User.is_active == True))
    users = result.scalars().all()
    print(f"Total active users: {len(users)}")
```

## 📚 Дополнительные скрипты

### Обновление профилей

```bash
# Обновить все профили
python scripts/fill_profiles.py --all

# Обновить конкретного пользователя
python scripts/fill_profiles.py --user-id 123456

# С настройкой размера пакета
python scripts/fill_profiles.py --all --batch-size 50
```

### Статистика по полу

```bash
python scripts/gender_stats.py
```

### Поиск пользователей YouTube в Telegram

```bash
python scripts/find_yt_users_in_telegram.py
```

## 🎉 Готово!

Теперь у вас:
- ✅ Все пользователи из каналов reLove в БД
- ✅ Заполненные профили с анализом LLM
- ✅ Возможность поиска и рекомендаций
- ✅ Готовность к таргетированным рассылкам

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `logs/fill_profiles_from_channels.log`
2. Запустите тест: `python scripts/test_telethon_connection.py`
3. Проверьте настройки в `.env`
4. Убедитесь, что вы участник каналов

---

**Создано для проекта reLove Bot** 🔥
